# cns-explainer
A browser-level standard for session-aware ambient content gating. Reduces workflow interruption by aligning ad, notification, and feed delivery with the user's active navigation context.
